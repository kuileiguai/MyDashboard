# 自建 Linux 开发者 Dashboard — 需求文档 v1.0

> 本文档由 `PRD.md`（痛点驱动的需求清单，7 模块 / 64 FR）与 `run.md`（工程化模块拆分 + API + 实施顺序）合并而成，是当前**唯一有效的需求基线**。原两份文档保留作历史参考，不再单独维护。
>
> 编号规则：`M{模块号}-F{功能序号}`。优先级：**P0 = MVP 必做**；**P1 = V1 必做**；**P2 = V2 或按需**。每条 FR 标注来源（PRD / run / 合并 / 新增）。

---

## 0. 合并说明（相对原文档的关键变化）

| # | 变化 | 说明 |
|---|------|------|
| 1 | 模块结构 | 采用 run.md 的 M0–M9 十模块结构；PRD 的 A–G 模块整体映射进入 |
| 2 | 终端方案 | **以 run.md 的内嵌 xterm.js 终端为主方案**（命名/分组/持久化原生解决痛点5）；PRD 的"外控 GNOME 终端窗口"降级为 P2 可选 |
| 3 | 文件管理 | 以 run.md 的**面板内文件管理器**为主（直接解决痛点2/3/7）；PRD 的"检测已开 Nautilus 窗口"降级为 P2 可选 |
| 4 | 新增模块 | M8 SSH 管理、M9 首页面板整体采纳 run.md 方案 |
| 5 | PRD 独有项保留 | systemd 管理、僵尸进程识别、端口快照、tmux 集成、CUDA 兼容、多日志聚合、时间线对齐等作为各模块 P2 高级项保留 |
| 6 | 项目概念 | PRD 的"项目为一等公民"保留为 P2 跨模块能力，MVP 不阻塞 |
| 7 | FR 重新编号 | 全部按 M{mod}-F{n} 重排，共 **92 条 FR**（P0=25 / P1=36 / P2=31） |

---

## 1. 背景与目标

### 1.1 用户与场景
- 单用户（开发者本人），Ubuntu 22.04 桌面版（GNOME）开发与测试。
- 公司 AI 产品/服务跑在同系统服务器上，通过多个终端执行命令管理。
- 同时维护多个项目，每个项目由 1–N 个服务进程组成，常开多个终端 + 多个文件管理器窗口。

### 1.2 起源痛点（11 条）

| # | 痛点 | 来源 |
|---|------|------|
| 1 | 常用指令记不住，反复上网查，缺解释 | 用户 |
| 2 | 文件夹开多了找不到，希望有管理页 + 拖拽 | 用户 |
| 3 | 新建文件要靠命令行 | 用户 |
| 4 | 端口占用查询：哪个端口、哪个服务、哪启动 | 用户 |
| 5 | 多终端开了不知道哪个跑啥，希望可命名 | 用户 |
| 6 | 命令历史搜索弱、alias 散乱、长命令反复拼 | 补充 |
| 7 | 路径复制 / 深目录跳转麻烦 | 补充 |
| 8 | 后台 / 僵尸进程管理、systemd 服务自启 | 补充 |
| 9 | SSH / tmux 远程会话散乱 | 补充 |
| 10 | 多 venv / Node 版本切换、.env 串味 | 补充 |
| 11 | 多服务日志分散、GPU/CPU/磁盘监控 | 补充 |

### 1.3 一句话目标
> 一个本地 Web 工作台，把「命令 / 文件 / 端口 / 终端 / 监控 / 日志 / 环境 / SSH」统一到 `http://localhost:8080` 一个页面里，可看、可搜、可点、可拖。

### 1.4 非目标（明确不做）
- 不做多人协作 / 权限系统 / 云同步。
- 不替代 IDE；不追求替代 Nautilus 全部能力，只做高频路径。
- 不替代专业监控（Prometheus/Grafana），只做"够用、即时、本地"。
- MVP 阶段不做远程主机管理（M8 SSH 是"发起连接"，不是"管理远端面板"）。

---

## 2. 概念模型

- **终端标签页 (Terminal Tab)**：面板内一个独立 PTY 会话，有名字、工作目录、状态灯。是"终端可命名"痛点的最终载体。
- **会话 (Session)**：一组终端标签页的快照，页面刷新/重开后可恢复。
- **快捷位置 (Bookmark)**：常用目录书签，可拖拽文件过去完成复制。
- **命令条目 (Command)**：一条可检索的命令资产，含解释、参数、示例、收藏状态。
- **项目 (Project)**（P2）：跨模块聚合体 = 路径 + 终端组 + 环境 + 命令集合 + 日志集合。

---

## 3. 功能模块总览

| 模块 | 名称 | 解决的痛点 | FR 数 | P0/P1/P2 |
|------|------|-----------|-------|----------|
| M0 | 基础框架 | 全局 | 10 | 5 / 4 / 1 |
| M1 | 命令速查手册 | 1、6 | 11 | 6 / 2 / 3 |
| M2 | 文件管理器 | 2、3、7 | 10 | 0 / 7 / 3 |
| M3 | 端口占用监控 | 4、8 | 11 | 5 / 3 / 3 |
| M4 | 终端中心（核心） | 5、9 | 11 | 5 / 5 / 1 |
| M5 | 系统资源监控 | 11 | 9 | 4 / 5 / 0 |
| M6 | 日志查看器 | 11 | 10 | 0 / 7 / 3 |
| M7 | 环境管理 | 10 | 9 | 0 / 0 / 9 |
| M8 | SSH 连接管理 | 9 | 5 | 0 / 0 / 5 |
| M9 | 首页面板 | 全局 | 6 | 0 / 3 / 3 |
| **合计** | | | **92** | **25 / 36 / 31** |

---

## 4. 模块详述

### M0 · 基础框架

> 目标：所有模块的公共底座。

| ID | 名称 | 描述 | 优先级 | 来源 |
|----|------|------|--------|------|
| M0-F1 | 后端骨架 | FastAPI，端口 8080，CORS，模块化路由 | P0 | run |
| M0-F2 | 前端骨架 | Vue 3 + Vite + Element Plus，端口 5173（开发） | P0 | run |
| M0-F3 | 整体布局 | 左侧可折叠导航 + 右侧内容区；导航项：命令手册/文件管理/端口监控/终端中心/系统监控/日志查看/环境管理/SSH管理 | P0 | run |
| M0-F4 | 主题切换 | 深色/浅色切换，跟随系统默认值 | P1 | 合并（PRD G4） |
| M0-F5 | 通信基础设施 | REST API + WebSocket 通道封装（自动重连、心跳） | P0 | run |
| M0-F6 | 配置持久化 | SQLite 单文件存储所有配置与用户数据，支持导出备份 | P0 | PRD G6 |
| M0-F7 | 全局搜索 | 顶部一个搜索框，跨命令/文件/书签/终端检索 | P1 | PRD G2 |
| M0-F8 | 快捷键体系 | 唤起搜索、新建终端、切换标签等全局快捷键 | P1 | PRD G3 |
| M0-F9 | 通知中心 | 面板内通知列表：磁盘预警、日志告警、端口变化等 | P1 | PRD G5 |
| M0-F10 | 项目概念 | 跨模块聚合（路径+终端组+环境+命令+日志），切换项目即切换上下文 | P2 | PRD G1 |

### M1 · 命令速查手册

> 解决痛点 1、6。让"用过一次的命令"沉淀成可检索资产。

| ID | 名称 | 描述 | 优先级 | 来源 |
|----|------|------|--------|------|
| M1-F1 | 预置命令库 | 出厂 50+ 常用命令，分类：文件操作/网络/进程/磁盘/AI相关/Docker/Git | P0 | 合并（PRD A9 + run） |
| M1-F2 | 命令条目结构 | 每条含：命令、用途说明、参数逐项解释、使用示例 | P0 | 合并（PRD A10 + run） |
| M1-F3 | 关键词搜索 | 中英文模糊搜索（命令文本/说明/分类） | P0 | 合并（PRD A3 + run） |
| M1-F4 | 收藏与置顶 | 收藏常用命令并置顶展示 | P0 | 合并（PRD A11 + run） |
| M1-F5 | 自定义 CRUD | 用户新增/编辑/删除命令条目 | P0 | 合并（PRD A1 + run） |
| M1-F6 | 一键复制 | 每条命令旁"复制"按钮，写入剪贴板 | P0 | 合并（PRD A4 + run） |
| M1-F7 | 参数占位符 | 命令支持 `{{param}}` 占位符，复制/执行前弹窗填值 | P1 | PRD A5 |
| M1-F8 | 发送到终端执行 | 选定目标终端标签页，直接发送命令执行 | P1 | PRD A6（依赖 M4） |
| M1-F9 | 全局命令自动收录 | 四来源自动收录：面板终端(输入缓冲)/外部终端(发送成功)/命令手册(发送)/shell历史(后台增量同步 ~/.bash_history ~/.zsh_history，source=shell)，支持加常用，一键转存为条目 | P1 | PRD A7 + run |
| M1-F10 | 导入/导出 | JSON 导入导出，便于备份与分享 | P2 | PRD A8 |
| M1-F11 | 使用频次统计 | 记录复制/执行次数，输出 Top 榜（供 M9 首页使用） | P2 | PRD A12 |
| M1-F12 | 悬浮小屏 | pywebview 无边框置顶小窗，自动吸附活动终端（切到终端显示/切走隐藏），聚合模糊查找 + 常用 + 发送到活动终端，快捷键 Ctrl+Shift+Space 与托盘切换 | P2 | run |

**关键 AC**：搜"查端口"立刻得到 `ss -tlnp | grep :8080` + 逐参数解释 + 示例 + 一键复制。

### M2 · 文件管理器

> 解决痛点 2、3、7。主方案为**面板内文件管理器**——高频文件操作不再依赖 Nautilus。

| ID | 名称 | 描述 | 优先级 | 来源 |
|----|------|------|--------|------|
| M2-F1 | 目录树 | 左侧目录树，默认展开 `~` 与常用项目目录 | P1 | run |
| M2-F2 | 文件列表 | 右侧列表：图标 + 名称 + 大小 + 修改时间 | P1 | run |
| M2-F3 | 面包屑 + 路径输入 | 顶部面包屑导航 + 可直接输入路径跳转 | P1 | run |
| M2-F4 | 新建文件/文件夹 | 当前目录下输入名称直接创建 | P1 | 合并（PRD B5/B7 + run） |
| M2-F5 | 重命名/删除/复制/移动 | 完整文件操作集（删除需二次确认） | P1 | run |
| M2-F6 | 快捷位置栏 | 常用目录书签，点击跳转；支持把文件拖拽到书签上完成复制 | P1 | 合并（PRD B2/B9 + run） |
| M2-F7 | 右键菜单 | 在此打开终端（联动 M4）/ 用 VSCode 打开 / 复制绝对路径 | P1 | 合并（PRD B8 + run） |
| M2-F8 | 文件模板库 | 新建时可选模板：.py/.sh/Dockerfile/.env/docker-compose.yml 等 | P2 | PRD B6 |
| M2-F9 | 最近打开 | 记录最近打开/编辑的文件，供 M9 首页展示 | P2 | PRD B11 |
| M2-F10 | 外部窗口检测 | 列出桌面已开的 Nautilus 窗口并可聚焦/关闭（可选能力） | P2 | PRD B1/B3/B4 |

**关键 AC**：从桌面拖一个文件到面板书签上即完成复制；右键任意目录"在此打开终端"，M4 新开一个定位到该目录的标签页。

### M3 · 端口占用监控

> 解决痛点 4、8。

| ID | 名称 | 描述 | 优先级 | 来源 |
|----|------|------|--------|------|
| M3-F1 | 监听端口表格 | 列：端口号、协议(TCP/UDP)、状态、PID、进程名、启动命令、启动路径 | P0 | 合并（PRD C1/C2 + run） |
| M3-F2 | 搜索与过滤 | 按端口号搜索、按进程名过滤 | P0 | 合并（PRD C4 + run） |
| M3-F3 | 行展开详情 | 完整启动命令、工作目录、启动时间、启动用户、父进程链 | P0 | 合并（PRD C2/C6 + run） |
| M3-F4 | 一键 kill | 结束进程，二次确认弹窗，可选 SIGTERM/SIGKILL | P0 | 合并（PRD C3 + run） |
| M3-F5 | 自动刷新 | 5 秒轮询 + 手动刷新按钮 | P0 | run |
| M3-F6 | 常用端口高亮 | 8080/3000/5000/22 等可配置清单高亮 | P1 | run |
| M3-F7 | 完整进程列表 | 全量进程视图，按 CPU/内存排序，可搜索 | P1 | PRD C5 |
| M3-F8 | 端口冲突检测 | 输入端口预先检查占用情况（启动新服务前用） | P1 | PRD C7 |
| M3-F9 | systemd 服务管理 | 列出 user-level 服务，支持 start/stop/restart/enable | P2 | PRD C8 |
| M3-F10 | 僵尸/孤儿进程识别 | 标记疑似僵尸/孤儿进程，提示清理 | P2 | PRD C9 |
| M3-F11 | 端口快照对比 | 定期快照，对比哪些端口是新起/消失的 | P2 | PRD C10 |

**关键 AC**：看到 `:8000` 被占 → 展开看到 `python manage.py runserver`、工作目录 `/home/.../proj-a`、父进程链 → 点"结束"释放端口。

### M4 · 终端中心（核心模块）

> 解决痛点 5、9。**内嵌 xterm.js 终端为主方案**——命名/分组/持久化原生成立；外控系统终端窗口作为 P2 可选。

| ID | 名称 | 描述 | 优先级 | 来源 |
|----|------|------|--------|------|
| M4-F1 | 多标签 PTY 终端 | 每个标签页一个独立 PTY 会话（xterm.js 渲染，python pty 后端，WebSocket 传输） | P0 | run |
| M4-F2 | 标签自定义命名 | 双击标签名进入编辑（如"AI-推理服务"、"前端-nginx"） | P0 | run（解决痛点5） |
| M4-F3 | 标签状态显示 | 名称 + 当前工作目录（缩写）+ 状态灯（绿=空闲 / 黄=有输出） | P0 | run |
| M4-F4 | 分屏 | 当前终端区域水平/垂直分割 | P1 | run |
| M4-F5 | 新建终端选项 | 可选 shell、初始工作目录、初始命令 | P0 | run |
| M4-F6 | 终端列表侧边栏 | 列出所有已开终端，支持拖拽排序、点击切换 | P1 | run |
| M4-F7 | 批量关闭 | 一键关闭指定/全部终端 | P1 | run |
| M4-F8 | 会话持久化 | 页面刷新/重开后终端不丢失（WebSocket 重连 + 后端保留 PTY 与回滚缓冲） | P0 | run |
| M4-F9 | 按项目分组 | 终端标签可按项目分组显示（依赖 M0-F10，可先手动分组） | P1 | PRD D3 |
| M4-F10 | SSH 会话标记 | SSH 会话标签显示目标主机名，与本地终端视觉区分（联动 M8） | P1 | PRD D9 |
| M4-F11 | tmux 集成 | 列出/接管本机 tmux session | P2 | PRD D10 |

**关键 AC**：开 5 个标签分别命名 "proj-a/api"、"proj-a/worker"、"proj-b/web"，刷新页面后标签和会话原样恢复。

### M5 · 系统资源监控

> 解决痛点 11（监控部分）。

| ID | 名称 | 描述 | 优先级 | 来源 |
|----|------|------|--------|------|
| M5-F1 | CPU 实时曲线 | 总体 + 每核使用率 | P0 | run |
| M5-F2 | 内存 + Swap | 使用率曲线与当前值 | P0 | run |
| M5-F3 | 磁盘监控 | I/O 读写速率 + 各分区使用量 | P1 | run |
| M5-F4 | GPU 监控 | 每张卡：显存使用/总量、GPU 利用率、温度、功耗、风扇转速 | P0 | run（AI 场景核心） |
| M5-F5 | GPU 进程列表 | 每张卡上运行的进程：PID + 进程名 + 显存占用 | P1 | run |
| M5-F6 | 网络速率 | 实时上传/下载速率 | P1 | run |
| M5-F7 | 采样策略 | 数据保留最近 5 分钟，默认 2 秒刷新（可配） | P0 | run |
| M5-F8 | 磁盘大文件 Top | 占用最大的文件/目录排行，识别模型权重 | P1 | PRD F6 |
| M5-F9 | 磁盘空间预警 | 分区使用率超阈值（默认 90%）触发通知（联动 M0-F9） | P1 | PRD F7 |

**关键 AC**：面板顶部常驻 GPU/CPU/内存概览条，跑模型时一眼看到显存逼近上限。

### M6 · 日志查看器

> 解决痛点 11（日志部分）。

| ID | 名称 | 描述 | 优先级 | 来源 |
|----|------|------|--------|------|
| M6-F1 | 打开日志文件 | 路径输入或从 M2 文件管理器选择打开 | P1 | run |
| M6-F2 | 大文件支持 | >100MB 文件：后端分页/流式读取，前端虚拟滚动 | P1 | run |
| M6-F3 | 级别高亮 | 自动识别 ERROR(红)/WARN(黄)/INFO(蓝) | P1 | run |
| M6-F4 | 实时 tail 模式 | 类 `tail -f`，自动滚动到底部，可暂停 | P1 | run |
| M6-F5 | 正则搜索 | 支持正则，匹配行高亮 | P1 | run |
| M6-F6 | 过滤器 | 只显示包含关键词的行 / 只看 ERROR | P1 | run |
| M6-F7 | 多日志标签页 | 同时打开多个日志文件，标签切换 | P1 | run |
| M6-F8 | 多日志聚合视图 | 多个日志源在同一视图按时间交错显示 | P2 | PRD F1 |
| M6-F9 | 时间线对齐 | 多日志按时间戳对齐对照 | P2 | PRD F2 |
| M6-F10 | ERROR 告警 | 新日志出现 ERROR 关键字时触发通知（联动 M0-F9） | P2 | PRD F8 |

### M7 · 环境管理

> 解决痛点 10。

| ID | 名称 | 描述 | 优先级 | 来源 |
|----|------|------|--------|------|
| M7-F1 | Python 环境列表 | 列出系统所有 Python 解释器与 Conda 环境 | P2 | run |
| M7-F2 | 环境详情 | 名称、路径、Python 版本、已装包数量 | P2 | run |
| M7-F3 | 包列表 | 查看某环境 pip/conda 包列表，支持搜索 | P2 | run |
| M7-F4 | 当前激活环境 | 显示系统默认/当前激活环境 | P2 | run |
| M7-F5 | 导出 requirements | 一键生成某环境 requirements.txt 并下载 | P2 | run |
| M7-F6 | 环境对比 | 对比两个环境的包差异 | P2 | run |
| M7-F7 | Node 版本列表 | nvm/fnm 管理的 Node 版本与当前默认 | P2 | PRD E2 |
| M7-F8 | .env 查看/编辑 | 按目录查看与编辑 .env 键值对（显示打码开关） | P2 | PRD E3 |
| M7-F9 | CUDA 兼容信息 | CUDA/driver/torch/tf 版本展示与兼容性提示 | P2 | PRD E6 |

### M8 · SSH 连接管理

> 解决痛点 9。

| ID | 名称 | 描述 | 优先级 | 来源 |
|----|------|------|--------|------|
| M8-F1 | ssh config 可视化 | 可视化增删改 `~/.ssh/config` 主机条目 | P2 | run |
| M8-F2 | 连接信息保存 | 名称、IP、端口、用户名、密钥路径、跳板机 | P2 | run |
| M8-F3 | 一键连接 | 点击即在 M4 新开标签页执行 ssh（自动带上 M4-F10 标记） | P2 | run |
| M8-F4 | 连接状态指示 | 在线/离线（ping/端口探测） | P2 | run |
| M8-F5 | 远程路径收藏 | 常用远程路径快捷收藏 | P2 | run |

### M9 · 首页面板

> 全局入口，聚合各模块高频信息。

| ID | 名称 | 描述 | 优先级 | 来源 |
|----|------|------|--------|------|
| M9-F1 | 系统概览卡片 | CPU/内存/磁盘/GPU 一句话状态（数据来自 M5） | P1 | run |
| M9-F2 | 活跃终端列表 | 当前终端标签列表，点击快速跳转 M4 | P1 | run |
| M9-F3 | 最近文件/项目 | 最近打开的文件与常用目录（数据来自 M2-F9） | P1 | run |
| M9-F4 | 常用命令 Top5 | 按使用频次（数据来自 M1-F11） | P2 | run |
| M9-F5 | 一键操作按钮 | 清理 apt 缓存 / 磁盘占用 Top10 / 重启某服务（可配置） | P2 | run |
| M9-F6 | 自定义布局 | 卡片拖拽排列 | P2 | run |

---

## 5. 实施顺序与里程碑

> 实施顺序合并自 run.md 的推荐构建顺序与 PRD 的优先级体系。原则：**先跑通核心闭环（框架→终端→端口→命令），再铺广度。**

### Phase 0 — 基础框架（M0 P0）
交付：FastAPI + Vue 脚手架、布局、导航、REST/WS 通道、SQLite 初始化。

### Phase 1 — 终端中心（M4 P0）
交付：多标签 PTY 终端、命名、状态灯、新建选项、会话持久化。**最核心，先跑通。**

### Phase 2 — 端口监控（M3 P0）
交付：端口表格、搜索过滤、详情展开、一键 kill、自动刷新。**最实用，快速见效。**

### Phase 3 — 命令手册（M1 P0）
交付：预置命令库、条目结构、搜索、收藏、CRUD、一键复制。**最简单，快速出成果。**

### Phase 4 — 系统监控（M5 P0）
交付：CPU/内存/GPU 实时曲线 + 采样策略。

### 以上 = MVP（25 条 P0）

### Phase 5 — 文件管理器（M2 P1）
### Phase 6 — 日志查看器（M6 P1）
### Phase 7 — 首页面板（M9 P1）+ M0/M1/M3/M4/M5 的 P1 项
### Phase 8 — 环境管理（M7）+ SSH 管理（M8）
### Phase 9 — 高级特性（各模块 P2，按需挑选）

---

## 6. 非功能性需求

| 类别 | 要求 |
|------|------|
| 性能 | 首屏 <1s；端口/进程接口 <500ms；监控推送间隔默认 2s 可配；大日志文件打开不卡（流式 + 虚拟滚动） |
| 安全 | 仅监听 127.0.0.1；危险操作（kill、删除、覆盖）二次确认；M4 命令执行即真实 shell，前端须有"当前目录"可见性防误操作 |
| 资源占用 | 后端常驻内存 <150MB（含 PTY 池），空闲 CPU ≈ 0 |
| 可扩展 | 每模块独立路由 + 独立数据表 + 独立前端视图，可单独开关 |
| 部署 | venv + 一键启动脚本 + systemd user service 开机自启、崩溃重启 |
| 可观测 | 后端日志输出到 `~/.local/state/dev-dash/`，按天滚动 |
| 数据安全 | SQLite 单文件，支持一键导出 zip；.env 类敏感内容展示默认打码 |
| 兼容 | Ubuntu 22.04 + GNOME 42 为基准；M2-F10/M4 外控类功能对桌面环境敏感，标记为可选 |
| 前端兼容 | Chrome/Edge/Firefox 近两年版本 |

---

## 7. 待决策问题（剩余）

以下问题在原 PRD 中列出，本次合并后大部分已由 run.md 拍板，剩余项：

| # | 问题 | 当前倾向 | 状态 |
|---|------|----------|------|
| Q1 | 后端语言 | Python + FastAPI | **已决（run.md）** |
| Q2 | 终端方案 | 内嵌 xterm.js 为主，外控为 P2 可选 | **已决（本次合并）** |
| Q3 | 数据存储 | SQLite | **已决（run.md 隐含 + PRD）** |
| Q4 | 桌面环境绑定 | GNOME 为基准，外控功能抽象适配层 | **已决（本次合并）** |
| Q5 | GPU 厂商 | 仅 NVIDIA（pynvml / nvidia-smi） | **已决（run.md）** |
| Q6 | 项目概念粒度 | 一组目录（主仓+子模块+日志目录） | 待定（P2，不阻塞） |
| Q7 | 是否需要远程面板 | MVP 仅本地，远程留接口 | 待定（V2 再议） |
| Q8 | 后期是否套 Tauri 桌面壳 | 暂不需要，浏览器够用 | 待定（按需） |

---

## 8. 变更记录

| 版本 | 日期 | 变更 |
|------|------|------|
| v1.0 | 2026-08-11 | 合并 PRD.md（7模块/64FR）与 run.md（10模块/API/实施顺序），重排为 M0–M9 共 92 条 FR；终端与文件管理改以内嵌方案为主 |
